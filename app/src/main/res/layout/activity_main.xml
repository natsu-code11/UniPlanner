<!-- ============================================================
     FILE: activity_main.xml
     POSIZIONE: app/src/main/res/layout/
     SCOPO: Layout principale dell'app. Contiene due parti:
            1. FrameLayout — il contenitore dove vengono mostrati
               i Fragment (Home, Lezioni, Esami, Scadenze)
            2. BottomNavigationView — la barra in basso con i 4 tab
     LEZIONE DI RIFERIMENTO: L10 (Layouts), L12 (Fragments)
     ============================================================ -->

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- contenitore dove vengono caricati i Fragment -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- barra di navigazione in basso con i 4 tab -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
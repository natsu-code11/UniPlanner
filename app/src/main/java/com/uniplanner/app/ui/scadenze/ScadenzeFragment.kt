// ============================================================
// FILE: ScadenzeFragment.kt
// POSIZIONE: app/src/main/java/com/uniplanner/app/ui/scadenze/
// SCOPO: Schermata che mostra la lista delle scadenze ordinate
//        per data. L'utente può aggiungere progetti, pagamenti,
//        consegne e attività di studio con priorità.
//        Questa sezione verrà completata dalla Persona 3.
// LEZIONE DI RIFERIMENTO: L12 (Fragments), L13 (RecyclerView), L15 (Room)
// ============================================================

package com.uniplanner.app.ui.scadenze

import android.os.Bundle
import android.view.View
import androidx.fragment.app.Fragment
import com.uniplanner.app.R

class ScadenzeFragment : Fragment(R.layout.fragment_scadenze) {  // collega questo Fragment al suo layout XML

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
        // il codice verrà completato dalla Persona 3
    }
}
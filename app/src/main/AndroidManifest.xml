<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniPlanner">

        <!-- LoginActivity disattivata temporaneamente -->
        <!-- <activity android:name=".LoginActivity" /> -->

        <!-- Activity configurazione iniziale -->
        <activity
            android:name=".ConfigurazioneActivity"
            android:exported="false" />

        <!-- Activity per aggiungere un esame -->
        <activity
            android:name=".ui.esami.AggiungiEsameActivity"
            android:exported="false" />

        <!-- Activity per aggiungere una lezione -->
        <activity
            android:name=".ui.lezioni.AggiungiLezioneActivity"
            android:exported="false" />

        <!-- Activity per aggiungere una scadenza -->
        <activity
            android:name=".ui.scadenze.AggiungiScadenzaActivity"
            android:exported="false" />

        <!-- Activity principale — punto di ingresso dell'app -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>